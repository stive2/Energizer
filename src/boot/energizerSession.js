import { defineBoot } from '#q-app/wrappers'

/**
 * @deprecated Les navigation guards sont enregistrés via `setupRouterGuards` dans `router/index.js`.
 * Conservé pour compatibilité si réactivé dans quasar.config.js boot.
 */
export default defineBoot(() => {})
